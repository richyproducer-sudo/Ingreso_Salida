{
  "name": "ingreso-salida",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT -c green,cyan \"npm:dev --prefix server\" \"npm:dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "download:models": "node scripts/download-models.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
